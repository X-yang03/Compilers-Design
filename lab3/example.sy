int a = 0s123;
int b = 0x123 ;0123
int c = 0X456;

// */ // comment, not syntax error
f = g/*/\aaaaaaaaa    




a\n*//h; // equivalent to f = g / h;
/*//*/ l(); // equivalent to l();
m = n//**/o
+ p; // equivalent to m = n + p;
